services:
  - type: web
    name: physio-whisper-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:10000 backend.App:app
    envVars:
      - key: FLASK_ENV
        value: production
