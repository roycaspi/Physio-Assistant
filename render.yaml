services:
  - type: web
    name: physio-whisper-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/App.py
    envVars:
      - key: FLASK_ENV
        value: production
